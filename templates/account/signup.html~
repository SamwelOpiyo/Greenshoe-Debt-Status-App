{% extends "account/basic.html" %}

{% load i18n %}

{% block head_title %}{% trans "Signup" %}{% endblock %}

{% block content1 %}
<h1 class="text-center">{% trans "Sign Up" %}</h1>

 
  
  
<div class="container">
	<div class="login-screen">
		<form class="signup" id="signup_form" method="post" action="{% url 'account_signup' %}">
			<div class="col-sm-12">
				<!--<div class="logo">
					<img src="https://github.com/hassan-siraj/Login-Screen/blob/master/logo.png?raw=true" alt="" width="100%">
				</div>-->
				<h1>Create an Account</h1>
  				{% csrf_token %}
  				{{ form.as_p }}
  				{% if redirect_field_value %}
  					<input type="hidden" name="{{ redirect_field_name }}" value="{{ redirect_field_value }}" />
  				{% endif %}
  				<button class="bttn" type="submit">{% trans "Sign Up" %} &raquo;</button>
  			</div>
			<div class="col-sm-12 login-footer">
				<div class="row">
					<div class="col-xs-12 text-center"> Already have an account?<a href="{{ login_url }}"> Sign In!</a> </div>
				</div>
			</div>
		</form>
	</div>
</div>




			
				
				
				<!--<input type="text" placeholder="Email" name="uname" required>
				<input type="password" placeholder="Password" name="psw" required>
				<button type="submit">Login</button>-->
			
		
	
<style type="text/css">
.login-screen { position: relative; height: 520px; top: 50%; width: 580px; left: 45vh; margin-left: 0px; }
.login-screen h1 { color: #000; text-align: center; font-size: 20px; }
.login-screen label { font-weight: normal; }
.logo { width: 200px; margin: 5px auto; }
form { border: 2px dashed #000; display: inline-block; max-width: 570px; background: rgba(255,255,255,.8); padding: 3%}
input[type=text], input[type=password], input[type=email] { color: #000; width: 100%; background: transparent; padding: 10px 15px; margin: 8px 0; display: inline-block; border: 1px dashed #000; box-sizing: border-box; outline: none; }
.bttn { background-color: #000; font-size: 14px; color: #fff; padding: 12px 15px; margin: 8px 0; border: none; cursor: pointer; width: 100%; }
.bttn:hover { background-color: #689f38; font-size: 14px; color: #000; padding: 12px 15px; margin: 8px 0; border: none; cursor: pointer; width: 100%; }
.login-footer { background-color: rgba(241, 241, 241, 0.62); padding: 15px; margin-top: 30px; }
.login-footer a { color: #689f38; font-size: 13px; }
/**For Ipad**/
@media (max-width: 991px) {
.login-screen { left: 1vh; margin-left: 0px; width: 380px;}
}
/**For All Small Devices**/
@media (max-width: 767px) {
form { border: 2px dashed #000; display: inline-block; max-width: 290px; background: rgba(255,255,255,.8); margin-left: auto; margin-right: auto; }
.login-screen { left: 50%; margin-left: -145px; }
}
::-webkit-input-placeholder { color: rgba(0,0,0,.7); }
::-moz-placeholder { color: rgba(0,0,0,.7); }
:-ms-input-placeholder { color: rgba(0,0,0,.7); }
:-moz-placeholder { color: rgba(0,0,0,.7); }
ul.errorlist {
    margin: 0;
    padding: 0;
    display: inline-block;
}
ul.errorlist li {
	 
    color: red;
    font-size: 12px;
}
</style>

{% endblock %}
