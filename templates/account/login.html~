{% extends "account/base.html" %}

{% load i18n %}
{% load account socialaccount %}

{% block head_title %}{% trans "Sign In" %}{% endblock %}

{% block content1 %}

<h1 class="text-center">{% trans "Sign In" %}</h1>

<div class="container">
	<div class="login-screen">
		<form class="login" method="POST" action="{% url 'account_login' %}">
			<div class="col-sm-12">
				<!--<div class="logo">
					<img src="https://github.com/hassan-siraj/Login-Screen/blob/master/logo.png?raw=true" alt="" width="100%">
				</div>-->
				<h1>Login to your Account</h1>
  				{% csrf_token %}
  				{{ form.as_p }}
  				{% if redirect_field_value %}
  					<input type="hidden" name="{{ redirect_field_name }}" value="{{ redirect_field_value }}" />
  				{% endif %}
  				<button class="bttn primaryAction" type="submit">{% trans "Sign In" %}</button>
  				
  				<a class="btn btn-block btn-social btn-twitter" href="/accounts/twitter/login/?process=login">
          		<i class="fa fa-twitter"></i>
          		Sign in with Twitter
        		</a>
        		<a class="btn btn-block btn-social btn-facebook" href="/accounts/facebook/login/?process=login">
          		<i class="fa fa-facebook"></i>
          		Sign in with Facebook
        		</a>
  			</div>
			<div class="col-sm-12 login-footer">
				<div class="row">
					<div class="col-xs-6"> <a class="button secondaryAction" href="{% url 'account_reset_password' %}">{% trans "Forgot Password?" %}</a> </div>
					<div class="col-xs-6 text-right"> New User?<a href="{{ signup_url }}"> Create an Account!</a> </div>
				</div>
			</div>
		</form>
	</div>
</div>




			
				
				
				<!--<input type="text" placeholder="Email" name="uname" required>
				<input type="password" placeholder="Password" name="psw" required>
				<button type="submit">Login</button>-->
			
		
	
<style type="text/css">
.login-screen { position: relative; height: 520px; top: 50%; width: 480px; left: 50vh; margin-left: 0px; }
.login-screen h1 { color: #000; text-align: center; font-size: 20px; }
.login-screen label { font-weight: normal; }
.logo { width: 200px; margin: 5px auto; }
form { border: 2px dashed #000; display: inline-block; max-width: 470px; background: rgba(255,255,255,.8); padding: 3%}
input[type=text], input[type=password] { color: #000; width: 100%; background: transparent; padding: 10px 15px; margin: 8px 0; display: inline-block; border: 1px dashed #000; box-sizing: border-box; outline: none; }
.bttn { background-color: #000; font-size: 14px; color: #fff; padding: 12px 15px; margin: 8px 0; border: none; cursor: pointer; width: 100%; }
.bttn:hover { background-color: #689f38; font-size: 14px; color: #000; padding: 12px 15px; margin: 8px 0; border: none; cursor: pointer; width: 100%; }
.login-footer { background-color: rgba(241, 241, 241, 0.62); padding: 15px; margin-top: 30px; }
.login-footer a { color: #689f38; font-size: 13px; }
/**For Ipad**/
@media (max-width: 991px) {
.login-screen { left: 2vh; margin-left: 0px; width: 350px}
}
/**For All Small Devices**/
@media (max-width: 767px) {
form { border: 2px dashed #000; display: inline-block; max-width: 290px; background: rgba(255,255,255,.8); margin-left: auto; margin-right: auto; }
.login-screen { left: 50%; margin-left: -145px; }
}
::-webkit-input-placeholder { color: rgba(0,0,0,.7); }
::-moz-placeholder { color: rgba(0,0,0,.7); }
:-ms-input-placeholder { color: rgba(0,0,0,.7); }
:-moz-placeholder { color: rgba(0,0,0,.7); }
ul.errorlist {
    margin: 0;
    padding: 0;
    display: inline-block;
}
ul.errorlist li {
	 
    color: red;
    font-size: 12px;
}
</style>


{% endblock %}
